// This file is part of the Loobee package.
//
// (c) Andrey Savitsky <contact@qroc.pro>
//
// For the full copyright and license information, please view the LICENSE
// file that was distributed with this source code.

/// Always use bit shifts instead idiv instruction.
@inline(never)
@usableFromInline
internal func divisionBy60000000000(_ count: Int64) -> Int64 {
    return count / 60000000000
}

/// Always use bit shifts instead idiv instruction.
@inline(never)
@usableFromInline
internal func divisionBy3600000000(_ count: Int64) -> Int64 {
    return count / 3600000000
}

/// Always use bit shifts instead idiv instruction.
@inline(never)
@usableFromInline
internal func divisionBy3600000000000(_ count: Int64) -> Int64 {
    return count / 3600000000000
}

/// Always use bit shifts instead idiv instruction.
@inline(never)
@usableFromInline
internal func divisionBy86400000000(_ count: Int64) -> Int64 {
    return count / 86400000000
}

/// Always use bit shifts instead idiv instruction.
@inline(never)
@usableFromInline
internal func divisionBy86400000000000(_ count: Int64) -> Int64 {
    return count / 86400000000000
}

/// Always use bit shifts instead idiv instruction.
@inline(never)
@usableFromInline
internal func divisionBy604800000000(_ count: Int64) -> Int64 {
    return count / 604800000000
}

/// Always use bit shifts instead idiv instruction.
@inline(never)
@usableFromInline
internal func divisionBy604800000000000(_ count: Int64) -> Int64 {
    return count / 604800000000000
}

/// Always use bit shifts instead idiv instruction.
@inline(never)
@usableFromInline
internal func divisionBy2629746000(_ count: Int64) -> Int64 {
    return count / 2629746000
}

/// Always use bit shifts instead idiv instruction.
@inline(never)
@usableFromInline
internal func divisionBy2629746000000(_ count: Int64) -> Int64 {
    return count / 2629746000000
}

/// Always use bit shifts instead idiv instruction.
@inline(never)
@usableFromInline
internal func divisionBy2629746000000000(_ count: Int64) -> Int64 {
    return count / 2629746000000000
}

/// Always use bit shifts instead idiv instruction.
@inline(never)
@usableFromInline
internal func divisionBy31556952000(_ count: Int64) -> Int64 {
    return count / 31556952000
}

/// Always use bit shifts instead idiv instruction.
@inline(never)
@usableFromInline
internal func divisionBy31556952000000(_ count: Int64) -> Int64 {
    return count / 31556952000000
}

/// Always use bit shifts instead idiv instruction.
@inline(never)
@usableFromInline
internal func divisionBy31556952000000000(_ count: Int64) -> Int64 {
    return count / 31556952000000000
}
